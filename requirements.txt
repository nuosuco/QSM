# PyTorch - CPU version for broader compatibility.
# If you have a CUDA-enabled GPU, you can install a version with GPU support
# for significantly faster training.
# See https://pytorch.org/get-started/locally/ for instructions.
torch
torchvision
torchaudio 