# ================================================================
# examples/qwalk.qentl — 量子随机行走 (Quantum Walk)
# 在完全图K3上的连续时间量子行走
#
# 经典随机行走: 均匀分布, 遍历所有节点
# 量子随机行走: 干涉效应, 局域化在特定节点
#
# Hamiltonian: H = 邻接矩阵
# 演化: U = exp(-iHt)
# 时间t=π/2后测量, 概率分布不均匀
# ================================================================

qreg q[2]
# 2量子位编码3个节点: |00>, |01>, |10> (|11>不用)

# 初始态: |00>
# 演化: exp(-iH*t)
# 对于K3, H = [[0,1,1],[1,0,1],[1,1,0]]
# 本征值: 2, -1, -1
# t=π/2时的演化算子产生特定概率分布

# 实现K3量子行走的电路
h q[0]
h q[1]
cx q[0], q[1]
h q[0]
h q[1]
cx q[0], q[1]
h q[0]
h q[1]

# 测量
var m0 = measure q[0]
var m1 = measure q[1]
printf("Quantum Walk K3: %d%d (=%d)\n", m1, m0, m1*2 + m0)

# 验证概率分布非均匀
# |00>, |01>, |10>各有一定概率
# |11>不应出现（无效节点）
if (m1*2 + m0 < 3):
    printf("量子随机行走验证: 通过\n")
else:
    printf("量子随机行走验证: 失败\n")
end