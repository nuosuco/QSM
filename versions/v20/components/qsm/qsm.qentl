# ================================================================
# lib/qsm.qentl — QSM主模型 v14
# 四大模型集成入口：QSM主模型 + SOM经济 + WeQ社交 + Ref自反省
# ================================================================

# 初始化QSM（集成四大模型）
def qsm_init():
    printf("QSM主模型初始化 v14\n")
    printf("四大模型：QSM(主) + SOM(经济) + WeQ(社交) + Ref(自反省)\n")
    return 1
end

# QSM主模型功能
def qsm_main():
    printf("QSM主模型运行中...\n")
    return 1
end

# 经济模型入口
def qsm_economy():
    printf("SOM经济模型：调用components/som/som.qentl\n")
    return 1
end

# 社交模型入口
def qsm_social():
    printf("WeQ社交模型：调用components/weq/weq.qentl\n")
    return 1
end

# 自反省模型入口
def qsm_reflect():
    printf("Ref自反省模型：调用components/ref/ref.qentl\n")
    return 1
end

# 健康检查
def qsm_health():
    var ok = 0
    if (file_exists("components/qcl/qcl.qentl")):
        ok = ok + 1
    end
    if (file_exists("components/qvm/qvm.qentl")):
        ok = ok + 1
    end
    if (file_exists("components/qdfs/qdfs.qentl")):
        ok = ok + 1
    end
    if (file_exists("components/qns/qns.qentl")):
        ok = ok + 1
    end
    if (file_exists("components/som/som.qentl")):
        ok = ok + 1
    end
    if (file_exists("components/weq/weq.qentl")):
        ok = ok + 1
    end
    if (file_exists("components/ref/ref.qentl")):
        ok = ok + 1
    end
    printf("QSM健康: %d/7 通过\n", ok)
    return ok
end

# 运行所有模型
def qsm_run_all():
    qsm_init()
    qsm_main()
    qsm_economy()
    qsm_social()
    qsm_reflect()
    return qsm_health()
end

# 主入口

# ---- QNS真实生长: 四大模型集成测试 ----
def qsm_test_all_models():
    printf("=== QSM四大模型集成测试 ===\n")
    
    # 测试SOM经济模型
    var som_ok = 0
    if (file_exists("components/som/som.qentl")):
        som_ok = 1
    end
    printf("SOM经济模型: %s\n", if (som_ok == 1) "通过" else "缺失" end)
    
    # 测试WeQ社交模型
    var weq_ok = 0
    if (file_exists("components/weq/weq.qentl")):
        weq_ok = 1
    end
    printf("WeQ社交模型: %s\n", if (weq_ok == 1) "通过" else "缺失" end)
    
    # 测试Ref自反省模型
    var ref_ok = 0
    if (file_exists("components/ref/ref.qentl")):
        ref_ok = 1
    end
    printf("Ref自反省模型: %s\n", if (ref_ok == 1) "通过" else "缺失" end)
    
    printf("=== 测试完成 ===\n")
    return som_ok + weq_ok + ref_ok
end

def qsm_main_entry():
    return qsm_run_all()
end
