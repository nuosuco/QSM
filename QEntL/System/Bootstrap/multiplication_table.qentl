# 九九乘法表 — QEntL自举验证
# 输出1*1到3*3的结果

主函数: 函数() {
    让 i = 1
    当(i <= 3) {
        让 j = 1
        当(j <= i) {
            让 r = i * j
            打印(r)
            j = j + 1
        }
        i = i + 1
    }
}
