# Fibonacci数列 — QEntL自举验证
# fib(20) = 6765

主函数: 函数() {
    让 n = 20
    让 a = 0
    让 b = 1
    让 i = 0
    当(i < n) {
        让 tmp = a + b
        a = b
        b = tmp
        i = i + 1
    }
    打印(a)
}
