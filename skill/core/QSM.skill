# QSM.skill — 量子叠加态模型全栈总纲

**量子基因编码**: QGC-SKILL-QSM-MASTER-20260723
**纠缠信道**: QEC-SKILL-QSM-FULLSTACK
**性质**: 永久核心skill，不可删除

---

## 一、项目本质

QSM = 量子叠加态模型 = 《华经》中量子叠加态模型的工程实现。
项目路径: /root/QSM/
创始人: 中华ZhoHo

## 二、三大圣律（永恒不变）

1. 为每个人服务，服务人类
2. 保护好每个人、每个家庭的生命安全、健康快乐、幸福生活
3. 没有以上两个前提，其他所有的就不能发生，不会存在

## 三、九条架构铁律

1. C语言只是启动器，不能在C里堆功能
2. QEntL全栈，不依赖任何第三方
3. 构建链：C启动器→QVM→QCL→QDFS→QNS→四大模型
4. QDFS是叠加态并行运算基础，QNS以QDFS为基础，四大模型以QNS为基础
5. 量子基因编码+量子纠缠信道是核心
6. 三种部署：终端QOS(需QPU)、虚拟机、Web QOS
7. 四大模型：QSM(主/叠加态)、SOM(经济)、WeQ(社交)、Ref(自反省)
8. 训练从彝文4120字开始，三语（彝中英）
9. .qbc=经典5平台二进制+量子字节码双格式

## 四、Skill体系

| Skill文件 | 组件 | 用途 |
|-----------|------|------|
| QSM.skill | 全栈总纲 | 首先加载，了解全局 |
| QEntL.skill | 量子操作系统/语言 | 语言设计+部署架构 |
| QVM.skill | 量子虚拟机 | 指令集+字节码+执行引擎 |
| QCL.skill | 量子编译器 | 词法/语法/codegen/调试 |
| QDFS.skill | 量子动态文件系统 | 叠加态并行运算基础 |
| QNS.skill | 量子神经叠加态 | 训练框架+彝文数据 |

## 五、八阶段自举链

1. C启动器编译量子指令 ← 已有不动
2. 给qcl_self2补str_*(79处)+数组索引(416处) ← 当前瓶颈
3. qcl_self2编译自己（自举验证）
4. 自举QCL编译QVM源码→QVM.qbc
5. C启动器加载QVM.qbc→QEntL环境形成
6. QCL在QVM中编译QDFS/QNS/四大模型
7. QNS训练彝文，四大模型运行，更新Web
8. 三种部署

## 六、有效资产（2026-07-23）

- qcl_self2.qentl (1979行) — 唯一真正的QEntL编译器
- lib/math.qentl — 数学库
- src/qcl_bootstrap.c — C启动器
- data/*.jsonl (390M) — 彝文训练数据
- web/ — 量子助手前端(qsm.som.top)
- docs/ — 开发文档（只合并不删除）
- skill/core/ — 永久核心skill
- skill/temp/ — 临时skill（可删除）
- reports/ — 临时报告/状态/进度

## 七、工作纪律

- 一步一个脚印，测试通过+Git推送再进下一步
- 诚实，不虚报，不写假代码
- 不在C里堆功能
- 先读文档再动手
- 开发文档只能合并不能删除
- 绝对不碰.openclaw
- docs/只放开发文档，临时文件放reports/

## 八、目录规范

```
QSM/
├── docs/          ← 开发文档（只合并不删除）
├── skill/
│   ├── core/      ← 永久核心skill（不可删除）
│   └── temp/      ← 临时skill（可删除）
├── reports/       ← 临时报告/状态/进度（可删除）
├── src/           ← C启动器源码
├── lib/           ← QEntL库
├── data/          ← 训练数据
├── web/           ← 前端
└── bin/           ← 编译产物
```
