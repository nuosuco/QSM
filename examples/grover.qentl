# ================================================================
# examples/grover.qentl — Grover量子搜索算法
# 在4个候选(|00>,|01>,|10>,|11>)中搜索目标|11>
#
# 经典方法: 平均需2次查询, 最坏4次
# Grover:   1次量子迭代即100%命中 (2量子位最优迭代次数=1)
#
# 算法结构 (纯QEntL + QCL量子门语法):
#   1. 初始化: H⊗H 制造均匀叠加态
#   2. 预言机(oracle): CZ门标记目标|11> (相位翻转)
#   3. 扩散算子(diffusion): H-X-CZ-X-H (关于均值反演)
#   4. 测量: 坍缩到目标态
# ================================================================

qreg q[2]

# 1. 初始化叠加态
h q[0]
h q[1]

# 2. 预言机: 标记|11>, 翻转其相位
cz q[0], q[1]

# 3. 扩散算子: 关于|00>的反射
h q[0]
h q[1]
x q[0]
x q[1]
cz q[0], q[1]
x q[0]
x q[1]
h q[0]
h q[1]

# 4. 测量
var m0 = measure q[0]
var m1 = measure q[1]
printf("Grover搜索结果: %d%d\n", m0, m1)
if (m0 == 1 and m1 == 1):
    printf("命中目标|11>\n")
else:
    printf("未命中\n")
end
