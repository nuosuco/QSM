# QNT做市策略阈值配置 v0.0.5

> 这份文档用大白话解释每个参数，让你一看就懂

---

## 一、执行引擎阈值（直接影响交易）

### 1️⃣ 价差阈值 `spread_pct`
- **当前值**：0.05%
- **作用**：价差大于这个数才看
- **可改吗**：✅ 可以改
- **改了会怎样**：
  - 改成0.03% → 更敏感，更多机会（但可能亏钱）
  - 改成0.10% → 更保守，更少机会（但更稳）

**大白话**：这就像你买东西，价差太小你不感兴趣，价差大你才考虑。改成0.03%就是"稍微有点利差就看一眼"，改成0.10%就是"利差必须够大我才动"。

---

### 2️⃣ 净利阈值 `net_profit_pct`
- **当前值**：0.01%
- **作用**：净利大于这个数才交易
- **可改吗**：✅ 可以改
- **改了会怎样**：
  - 改成0 → 不亏钱就交易
  - 改成0.05% → 必须有利润才交易

**大白话**：这就像你做生意，0.01%就是"不亏就行"，0.05%就是"至少赚点才行"。改成0就是"只要不亏本我就做"。

---

### 3️⃣ 双边成本 `BI_SIDE_COST`
- **当前值**：0.12%
- **作用**：交易成本（手续费+滑点）
- **可改吗**：⚠️ 不建议改（这是交易所实际费率，改了会导致计算错误）

**大白话**：这是交易所收你的钱，你改不了。永续合约收0.04%，现货收0.02%，滑点再算0.02%，加起来单边0.06%，双边就是0.12%。这个是固定的，别改。

---

## 二、风控阈值（保护账户安全）

### 4️⃣ 最小净利 `MIN_NET_PROFIT_PCT`
- **当前值**：0.1%
- **作用**：风控级别的最小净利要求
- **可改吗**：✅ 可以改
- **改了会怎样**：
  - 改成0.05% → 更宽松
  - 改成0.2% → 更严格

**大白话**：这是风控层面的要求，比执行引擎的阈值更高。执行引擎说"0.01%就交易"，风控说"不行，至少0.1%才允许"。改成0.05%就是"风控放松一点"，改成0.2%就是"风控严格一点"。

---

## 三、模式发现阈值（分析用，不影响实盘）

### 5️⃣ 模式发现阈值 `spread_threshold` / `profitable_spread`
- **当前值**：0.08% / 0.10%
- **作用**：用于策略分析，不是直接控制交易
- **可改吗**：✅ 可以改，但不影响实盘交易

**大白话**：这两个参数是给"模式发现引擎"用的，用来分析历史数据找规律。不改它们，实盘交易照样跑。改了只是影响分析报告，不影响实际挂单。

---

## 四、当前市场状况

**时间**：2026-08-17 04:27 GMT+8

| 交易所 | 币种 | 价差 | 净利 | 状态 |
|--------|------|------|------|------|
| HTX | SOL | 0.116% | -0.004% | ❌ 亏 |
| Bitget | BTC | 0.051% | -0.069% | ❌ 亏 |
| HTX | ETH | 0.004% | -0.116% | ❌ 亏 |

**结论**：当前市场价差全部低于成本线0.12%，没有可盈利机会。

---

## 五、修改建议

### 方案A：激进型（追求机会）
```python
spread_pct = 0.03    # 原0.05
net_profit_pct = 0   # 原0.01
```
- 效果：最大机会，但可能亏钱

### 方案B：保守型（追求稳定）
```python
spread_pct = 0.10    # 原0.05
net_profit_pct = 0.05 # 原0.01
```
- 效果：稳，但机会少

### 方案C：当前型（等待机会）
- 不改阈值，等市场波动加剧时自动触发

---

> 最后更新：2026-08-17 04:27 GMT+8
> 维护者：小蕊Ref
