// QEntL标准库 v1.0

// 数学工具
绝对值: 函数(x) { 返回 绝对值(x) }
平方: 函数(x) { 返回 x * x }
立方: 函数(x) { 返回 x * x * x }
最大值三个: 函数(a, b, c) { 返回 最大值(a, 最大值(b, c)) }
最小值三个: 函数(a, b, c) { 返回 最小值(a, 最小值(b, c)) }
限制范围: 函数(x, lo, hi) {
    如果 x < lo { 返回 lo }
    否则如果 x > hi { 返回 hi }
    否则 { 返回 x }
}

// 字符串工具
是空或空白: 函数(s) { 返回 长度(删除空白(s)) == 0 }
开头是: 函数(s, prefix) { 返回 开始以(s, prefix) }
结尾是: 函数(s, suffix) { 返回 结束以(s, suffix) }

// 列表工具
平均值: 函数(arr) {
    如果 长度(arr) == 0 { 返回 0 }
    返回 求和(arr) / 长度(arr)
}
