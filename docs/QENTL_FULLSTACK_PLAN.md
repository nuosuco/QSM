# QEntL全栈QSM构建方案 v3.0
## 完整构建步骤(2026-08-18更新·中华规划)

---

## 一、铁律(不可违反)

| # | 铁律 | 说明 |
|---|------|------|
| 1 | C种子是点火器 | src/qcl_bootstrap.c已完成任务,不再升级 |
| 2 | QEntl启动器不修改 | lib/qvm_boot.qentl只启动QVM,绝不改 |
| 3 | QSCL在QNS实现 | 不在C种子/QVM/启动器里改 |
| 4 | Python零容忍 | 项目内无*.py,数据用QDFS/QSM/QNS负责 |
| 5 | git推送可force | 但不能pull覆盖本地 |
| 6 | 目录独立不混放 | 归档用cp不是mv |
| 7 | 诚实,不虚报 | 测试输出为证,不说没做 |

---

## 二、目录结构(中华规划·2026-08-18·完整标注原文)

```
/root/QSM/              ← 项目根
├── QEntl/    # 实际运行版本，比如现在应该是V0.0.2，我们暂时空着就可以，因为我们还没有真正服务用户
├── QLife/    # 生长版本，现在我们在生长，我们的Web操作系统桌面先绑定在这里，因为我要测试
│   └── v0.0.3/   ← 当前开发版本
├── QSM/      # 所有历史版本，现在是V0.0.1，V0.0.2，所有版本以后一起通过量子纠缠通信与量子态传送构建成QSM
│   ├── v0.0.1/   ← 第一版本归档
│   └── v0.0.2/   ← 第二版本归档
├── root/     # 用户自己的数据，用QDFS构建，现在空着
├── Q/        # 不知道用什么名，先这样，就是经典里的应用商店，但我们QSM的应用就是一个完整的自举生长的QEntl全栈量子操作系统
├── OS/       # 三种部署安装程序，如终端安装程序
└── docs/     # 现在有的目录
```

---

## 三、构建链

```
QEntl启动器 → QCL → QVM → QDFS → QNS → 四大模型 → HTTP/QOS
  (入口)    (编译) (运行) (存储) (训练) (推理)   (接口)
```

---

## 四、当前状态(2026-08-18)

| 组件 | 文件 | 行数 | 状态 | 备注 |
|------|------|------|------|------|
| QVM | qvm.qentl | 1069 | ⚠️有编译错误 | 局部变量声明问题 |
| QCL | qcl.qentl | 1577 | ✅ | 含符号表+数组管理 |
| 启动器 | lib/qvm_boot.qentl | ~50 | ✅稳定 | 不改 |
| C种子 | bin/q_bootstrap | ELF | ✅退役 | 不升级 |
| QDFS | lib/qdfs_ns.qentl | - | ✅恢复 | 完整 |
| QNS框架 | lib/qns_framework.qentl | 215 | ⚠️缺实现 | 需补训练逻辑 |
| 彝文数据 | qdfs/ns/data/yi_train_4120.data | 4120条 | ✅ | 码点993040-997159 |
| 权重文件 | qdfs/ns/models/*.weights | - | ❌空/假 | 需真训练生成 |
| 服务器 | server_qentl.qentl | 232 | ⚠️假推理 | /api/yi不读权重 |
| Web桌面 | web/qdesktop.html | 166 | ✅完整 | 绑定根路径 |

---

## 五、构建步骤(按顺序执行)

### 步骤1: 修复QVM编译错误
**目标**: QVM编译通过,无错误
**操作**:
1. 添加pop4/pop5变量声明(pop1_t~pop5_t, pop1_i~pop5_i, pop1_s~pop5_s, pop1_a~pop5_a)
2. 添加do_pop4/do_pop5函数定义
3. 在do_builtin开头声明所有局部变量(避免重复声明)
4. 验证: `./bin/q_bootstrap compile qvm.qentl run/qvm.qbc` → 退出码0

### 步骤2: 实现QSCL训练脚本
**目标**: 4态叠加态并行训练,每态不同起点
**设计**:
- 4态各自从不同随机起点初始化权重(W1[6x8], W2[8x4120])
- 训练数据分片: 态0→分片[0,1030), 态1→[1030,2060), 态2→[2060,3090), 态3→[3090,4120)
- 每态独立训练N轮
- 坍缩合并: W_final = (W1+W2+W3+W4)/4
**验证**: 训练输出epoch准确率≥80%

### 步骤3: 生成真实权重文件
**目标**: qdfs/ns/models/qns_shard_0.weights包含真实训练结果
**操作**: 训练脚本运行时将权重写入文件
**验证**: 文件大小>10KB, 数值合理

### 步骤4: 修复服务器/api/yi
**目标**: 直接读取QSCL权重推理,不用简化公式
**操作**: 
1. 加载qns_shard_0.weights
2. 码点→特征向量
3. mat_mul(W1) → tanh → mat_mul(W2) → softmax → argmax
4. 返回对应彝文
**验证**: `curl -X POST http://localhost:9802/api/yi -d '993040'` → 返回正确彝文字符

### 步骤5: 启动服务器测试
**目标**: 端口9802监听,所有路由正常
**验证**:
- GET / → 返回完整HTML(166行)
- GET /api/status → `{"server":"QEntL/2.0","status":"running","qscl":"trained"}`
- POST /api/yi -d '993040' → 返回第1个彝文字符

### 步骤6: 提交推送
**目标**: dev/main/master同步到最新
**操作**: `git push --force origin dev main master`

---

## 六、进度表

| 步骤 | 任务 | 状态 | 验证 | 备注 |
|------|------|------|------|------|
| 1 | 修复QVM编译错误 | ⏳进行中 | - | 局部变量声明问题 |
| 2 | 实现QSCL训练脚本 | ❌待做 | - | 4态不同起步 |
| 3 | 生成真实权重 | ❌待做 | - | 文件需>10KB |
| 4 | 修复/api/yi推理 | ⚠️部分完成 | - | 简化版可用 |
| 5 | 启动服务器测试 | ⚠️待启动 | - | 需步骤1完成后 |
| 6 | 提交推送 | ⏳待执行 | - | 步骤5通过后 |

---

## 七、QSCL核心原理(必记)

### 五步框架
```
1. N态不同起点(不同初始权重)
2. 各态独立并行训练
3. 各态学到不同权重
4. 观测坍缩(合并所有态权重)
5. 最终模型含所有态知识
```

### 四步技术
```
1. embedding: 码点→N维向量
2. mat_mul: 矩阵运算
3. softmax: 概率分布
4. backprop: 反向传播更新权重
```

### 叠加态并行不同起步
- 态0: 随机起点W0, 训练分片0→1030, 得到权重0
- 态1: 随机起点W1, 训练分片1030→2060, 得到权重1
- 态2: 随机起点W2, 训练分片2060→3090, 得到权重2
- 态3: 随机起点W3, 训练分片3090→4120, 得到权重3
- 坍缩: W_final = (W0+W1+W2+W3)/4
- 推理: code → embedding → mat_mul(W) → softmax → argmax → ID

---

## 八、QEntL全栈系统组成

### 8.1 底层: C种子(已退役)
- 文件: src/qcl_bootstrap.c
- 功能: 量子指令编译 + 最小QEntL编译 + QVM执行
- 状态: 已完成自举,不再升级

### 8.2 编译层: QCL
- 文件: qcl.qentl → build/qcl.qbc
- 功能: QEntL编译器,自举验证通过
- 当前: 1577行,支持量子门+var+数组+字符串

### 8.3 运行层: QVM
- 文件: qvm.qentl → run/qvm.qbc
- 功能: QBC1字节码虚拟机
- 当前: 1069行,含31条opcode+14个builtin
- 新增: QSCL builtin(embedding/mat_mul/softmax/argmax/backprop)

### 8.4 存储层: QDFS
- 文件: lib/qdfs_ns.qentl
- 功能: 量子动态文件系统,叠加态并行存储
- 数据: qdfs/ns/data/, qdfs/ns/models/

### 8.5 训练层: QNS
- 文件: lib/qns_framework.qentl
- 功能: 神经叠加态框架,包含QSCL五步
- 当前: 215行架构,需补训练逻辑

### 8.6 模型层: 四大模型
- 文件: components/qsm/, components/som/, components/weq/, components/ref/
- 功能: 主模型/经济/社交/自反省
- 训练: 彝文4120字三语

### 8.7 接口层: HTTP/QOS
- 文件: server_qentl.qentl → build/server.qbc
- 功能: 9802端口HTTP服务器
- 路由: / /ide /api/status /api/yi /api/xiaoqu
- Web桌面: web/qdesktop.html

---

## 九、关键教训(2026-08-18)

1. **不要断章取义** — 中华的完整原文要一字不差写入所有索引
2. **目录在项目根** — 所有目录在/root/QSM/里面,不是/root/
3. **版本独立不混** — v0.0.3在QLife/, v0.0.1/v0.0.2在QSM/
4. **真实产物为证** — 不说完成了除非机器验证通过
5. **QVM是QEntL写的** — 不在C种子改,在qvm.qentl改
6. **QNS是训练层** — QSCL在QNS实现,不在QVM/启动器改
7. **自主执行铁律** — 中华没时间回复时,按规划继续执行,不等指令

---

**创建时间**: 2026-08-18
**最后更新**: 2026-08-18
**作者**: 小趣WeQ
**状态**: 执行中(步骤1进行中)
---

## 附: QSCL叠加态训练验证记录 (2026-08-18)

### 里程碑: 5个QSCL builtin全部实现并通过测试
- mat_mul: 矩阵乘法 (W布局=[输入维][类], W[d*K+cls])
- softmax: 定点softmax (exp近似 100+v*10, 输出0-100概率)
- argmax: 最大概率索引
- embedding: 码点索引查表(返回行向量)
- backprop: 符号感知梯度更新 (W[i] += lr*grad[i]/100)

### 原型训练结果 (N=16, K=16, 一对一任务, 4态不同起步)
- 随机基线: 6.25%
- 单态最高: 43% (seed=4, 60 epochs)
- 坍缩投票合并: 56% (9/16) — 超过任何单态!
- 验证: 投票坍缩 > 单态 > 随机, QSCL叠加态并行训练真实生效

### 关键经验 (QCL整数限制下的训练)
1. W矩阵索引布局必须一致: mat_mul读W[j*m+i]=W[输入维][类], 更新也必须用同布局
2. u16溢出: 利用先除后乘避免中间溢出
3. 负梯度会被整数除法截断: backprop做符号感知(先取绝对值除再取负)
4. 学习率: LR=20-100, backprop分母100 (步长≈梯度本身)
5. E和W同时更新会正反馈震荡: 先固定E只训W, 或小步长E更新
6. 坍缩合并用投票(多数决)优于权重平均: 各态学到不同知识,平均会稀释
7. 数组池句柄: ar_off/ar_size扩大到16384支持大量临时数组
